/**
 * REST controllers for the vendor portal API.
 * <p>
 * All controllers share the base path {@code /vendor} and expose endpoints for vendor
 * authentication, shop management, dashboard, menu, orders, payouts, reviews, settings,
 * and location services.
 */
package com.gutfriendly.app.vendor.controller;
